interface:
  display_name: "ManDiff"
  short_description: "用图表、场景与调用说明对照改动前后的系统，兼顾理解深度与生成速度"
  default_prompt: "Use $mandiff to explain both the original and changed system with aligned diagrams, tables, scenarios and call walkthroughs, compare the same inputs and results, and audit the exact diff in our conversation language with appropriate scope and detail."
