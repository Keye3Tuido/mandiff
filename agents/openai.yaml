interface:
  display_name: "ManDiff"
  short_description: "将大型改动转成跨 Agent 可移植、可视化的证据审阅指南"
  default_prompt: "Use $mandiff to prepare the selected change, author only its semantic review draft, and finalize the portable evidence-complete report with the bundled workflow."
